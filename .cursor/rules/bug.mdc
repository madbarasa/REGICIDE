UI/逻辑同步规则
单一数据源: UI显示必须完全基于逻辑状态
状态一致性: 任何状态变更必须同时更新UI和逻辑
事件隔离: 不同区域的元素使用独立的交互逻辑
视觉状态映射: 每个逻辑状态都有明确的视觉表示
卡牌交互规则
位置决定交互: 手牌区可选择，战场区只显示
状态决定可用性: 护盾牌不可再次选择
视觉即逻辑: 如果看起来不可用，就应该是不可用的
调试方法论
状态检查: 优先检查UI状态是否反映逻辑状态
事件追踪: 确认事件绑定是否符合预期
边界测试: 测试各种边缘情况和状态转换---
alwaysApply: true
---
